%ul.panel-list
  - @users.each do |user|
    %li 
      - link_to user, :method => :get, :remote => true, :class => 'get-item', :'data-type' => 'html' do
        %img{:src => profile_picture(user.email), :alt => user.email, :width => 48, :height => 48}
        .title= user.email rescue ''
        .comment= user.access_type rescue ''
