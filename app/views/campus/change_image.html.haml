:javascript
  $(document).ready(function() {
    parent.changeResourceImage("#{@campus.id}", "#{@campus.image_url(:small).to_s}", "#{@campus.image_url(:medium).to_s}");
  });
  $("#campus_image").live("change", function() {
    $('#upload-button').html('Cambiando...')
    $("#item-upload-image-form").submit();
  });
#change-image-box
  = form_for(@campus, :url => { :action => "upload_image" }, :html => {:id => 'item-upload-image-form', :multipart => true}) do |f|
    = hidden_field_tag :id, @campus.id
    = image_tag @campus.image_url.to_s

    #upload-pic-box
      = f.file_field :image
      #upload-button.upload Cambiar logotipo
