:javascript
  $(document).ready(function() {
    parent.changeResourceImage("#{@staff.id}", "#{@staff.image_url(:small).to_s}", "#{@staff.image_url(:medium).to_s}");
  });

  $("#staff_image").live("change", function() {
    $('#upload-button').html('Cambiando...')
    $("#item-upload-image-form").submit();
  });
#change-image-box
  = form_for(@staff, :url => { :action => "upload_image" }, :html => {:id => 'item-upload-image-form', :multipart => true}) do |f|
    = hidden_field_tag :id, @staff.id
    = image_tag @staff.image_url.to_s

    #upload-pic-box
      = f.file_field :image
      #upload-button.upload Cambiar fotografía
